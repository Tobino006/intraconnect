<resources>
    <!-- Status bar is transparent to allow drawing behind system bars -->
    <color name="status_bar_transparent">@android:color/transparent</color>

    <style name="Theme.IntraConnect.App" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor">@color/status_bar_transparent</item>
    </style>
</resources>